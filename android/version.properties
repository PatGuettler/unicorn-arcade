# Single source of truth for Play Store user-visible version (versionName).
# versionCode: CI passes -PversionCode from github.run_number (must always increase).
VERSION_NAME=2.0.0
# Fallback when building locally without -PversionCode
VERSION_CODE=2
